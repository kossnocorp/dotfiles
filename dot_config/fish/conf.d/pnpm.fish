# Aliases

alias p pnpm
# NOTE: Allow using Pi agent
# alias pi "pnpm install"
alias pil "pnpm install"
alias pa "pnpm add"
alias pad "pnpm add -D"
alias pr "pnpm remove"
alias pe "pnpm exec"
alias px pnpx
